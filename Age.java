public class Age{

    //No Constructor -- do you remember what this mean?
    //still makes default constructor
    public boolean isTeenAger(int age){
        return age >= 13 && age <= 19;
        // if (age >= 13 && age <= 19)
        //     return true;
        // return false;
    }
}